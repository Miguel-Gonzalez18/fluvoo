import BusinessLayout from "@/modules/dashboard/business/layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <BusinessLayout>{children}</BusinessLayout>;
}
